# exportReportToWord.ts

## Pseudocode

1. Receive the template name, report text, and placeholder context values.
2. Forward those values to the UWV Word export helper.
3. Return the helper result so the caller knows whether a matching export template existed.
