# reportApi.ts

## Pseudocode

1. Import the remote app-data function that saves a written report.
2. Expose a small `reportApi` object.
3. Map the `save` method directly to that remote save function.
