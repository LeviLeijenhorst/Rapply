import React from "react"
import Rapply from "./Rapply.svg"

export default function RapplyLogoComponent({ width = 300, height = 90 }: { width?: number; height?: number }) {
  return <Rapply width={width} height={height} />
}
