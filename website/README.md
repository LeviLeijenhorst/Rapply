# Coachscribe Website

This folder contains the Coachscribe marketing website built with Next.js (App Router) for good search engine visibility.

## Run locally

From the repo root:

```bash
cd website
npm run dev
```

Then open `http://localhost:3000`.

## Build

```bash
cd website
npm run build
npm run start
```
